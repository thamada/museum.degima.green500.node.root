#!/bin/sh -f

export TCLLIBPATH="none /usr/lib64 /usr/lib64 none"; \
export TK_LIBRARY="/usr/lib64"; \
export PATH="none":"/usr/bin":$PATH; \
exec ibis "$@" <<EOF

set argv0 ibdiagui
package require ibdiagui

EOF
