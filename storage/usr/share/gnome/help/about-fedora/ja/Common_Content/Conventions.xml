<?xml version='1.0' encoding='utf-8' ?>
<!DOCTYPE section PUBLIC "-//OASIS//DTD DocBook XML V4.5//EN" "http://www.oasis-open.org/docbook/xml/4.5/docbookx.dtd" [
<!ENTITY % BOOK_ENTITIES SYSTEM "../About_Fedora.ent">
%BOOK_ENTITIES;
<!ENTITY % BOOK_ENTITIES SYSTEM "Common_Content.ent">
%BOOK_ENTITIES;
]>

<section>
	<title>表記方法</title>
	<para>
		本ガイドは特定の単語や語句を強調したり、 記載内容の特定部分に注意を引かせる目的で次のような表記方法を使用しています。
	</para>
	<para>
		PDF版 および印刷版では、 <ulink url="https://fedorahosted.org/liberation-fonts/">Liberation Fonts</ulink> セットから採用した typefaces を使用しています。 ご使用のシステムに Liberation Fonts セットがインストールされている場合、 HTML 版でもこのセットが使用されます。 インストールされていない場合は代替として同等の typefaces が表示されます。 注記: Red Hat Enterprise Linux 5 およびそれ以降のバージョンにはデフォルトで Liberation Fonts セットが収納されます。
	</para>
	<section>
		<title>印刷における表記方法</title>
		<para>
			特定の単語や語句に注意を引く目的で 4 種類の表記方法を使用しています。 その表記方法および適用される状況は以下の通りです。
		</para>
		<para>
			<literal>Mono-spaced Bold</literal>
		</para>
		<para>
			シェルコマンド、 ファイル名、 パスなどシステムからの出力を強調するために使用しています。 またキー配列やキーの組み合わせを強調するのにも使用しています。 例えば、
		</para>
		<blockquote>
			<para>
				現在作業中のディレクトリ内のファイル <filename>my_next_bestselling_novel</filename> の内容を表示させるには、 シェルプロンプトで <command>cat my_next_bestselling_novel</command> コマンドを入力してから <keycap>Enter</keycap> を押してそのコマンドを実行します。
			</para>
		</blockquote>
		<para>
			上記にはファイル名、 シェルコマンド、 キーが含まれているのがわかります。 すべて Mono-spaced Bold で表されているため文中内で見分けやすくなっています。
		</para>
		<para>
			キーが 1 つの場合と複数のキーの組み合わせになる場合を区別するため、 その組み合わせを構成するキー同士をハイフンでつないでいます。 例えば、
		</para>
		<blockquote>
			<para>
				<keycap>Enter</keycap> を押してコマンドを実行します。 1 番目の仮想ターミナルに切り替えるには、
			</para>
			<para>
				<keycombo><keycap>Ctrl</keycap><keycap>Alt</keycap><keycap>F1</keycap></keycombo> を押します。 X-Windows セッションに戻るには、 <keycombo><keycap>Ctrl</keycap><keycap>Alt</keycap><keycap>F7</keycap></keycombo> を押します。
			</para>
		</blockquote>
		<para>
			最初の文では押すべき 1 つのキーを特定して強調しています。 次の文では同時に押すべき ３つのキーの組み合わせが 2 種類ありそれぞれ強調されています。
		</para>
		<para>
			ソースコードの説明では 1 段落内で提示されるクラス名、 メソッド、 関数、 変数名、 戻り値を上記のように <literal>Mono-spaced Bold</literal> で表示します。 例えば、
		</para>
		<blockquote>
			<para>
				ファイル関連のクラス群はファイルシステムに対しては <classname>filesystem</classname>、 ファイルには <classname>file</classname>、 ディレクトリには <classname>dir</classname> をそれぞれ含みます。 各クラスは個別に関連する権限セットを持っています。
			</para>
		</blockquote>
		<para>
			<application>Proportional Bold</application>
		</para>
		<para>
			アプリケーション名、 ダイアログボックスのテキスト、 ボタン名、 チェックボックスとラジオボタンのラベル、 メニュータイトルとサブメニュータイトルなどシステム上で見られる単語や語句を表します。 例えば、
		</para>
		<blockquote>
			<para>
				メインメニューバーから <guimenu>システム &gt; 個人設定 &gt; マウス</guimenu> の順で選択し <application>マウスの個人設定</application> を起動します。 <guilabel>ボタン</guilabel> タブ内の <guilabel>左ききのマウス</guilabel> チェックボックスをクリックしてから <guibutton>閉じる</guibutton> をクリックしマウスの第一ボタンを左から右に切り替えます (マウスを左ききの人が使用するのに適した設定にする)。
			</para>
			<para>
				<application>gedit</application> ファイルに特殊な文字を挿入する場合は、 メインメニューバーから <guimenu>アプリケーション &gt; アクセサリ &gt; 文字マップ</guimenu> の順で選択します。 次に <application>文字マップ</application> メニューバーから <guimenu>検索 &gt; 検索&hellip;</guimenu> と選択して <guilabel>検索</guilabel> フィールド内にその文字名を入力し <guibutton>次</guibutton> をクリックします。 探している文字が <guilabel>文字表</guilabel> 内で強調表示されます。 この強調表示された文字をダブルクリックすると <guilabel>コピーするテキスト</guilabel> フィールド内に置かれるので次に <guibutton>コピー</guibutton> ボタンをクリックします。 ここでドキュメントに戻り <application>gedit</application> メニューバーから <guimenu>編集 &gt; 貼り付け</guimenu> を選択します。
			</para>
		</blockquote>
		<para>
			上記には、 アプリケーション名、 システム全体のメニュー名と項目、 アプリケーション固有のメニュー名、 GUI インタフェースで見られるボタンやテキストがあります。 すべて Proportional Bold で表示されているため文中内で見分けやすくなっています。
		</para>
		<para>
			<guimenu> &gt;</guimenu> はメニューやそのサブメニュー間の移動や選択順序を示すための省略表現になります。 「メインメニューバーの <guimenu>システム</guimenu> メニュー内にある <guimenu>個人設定</guimenu> サブメニューから <guimenuitem>マウス</guimenuitem> を選択します」と言うような紛らわしい表現を避けるために使用しています。
		</para>
		<para>
			<command><replaceable>Mono-spaced Bold Italic</replaceable></command> または <application><replaceable>Proportional Bold Italic</replaceable></application>
		</para>
		<para>
			Mono-spaced Bold、 Proportional Bold いずれであっても斜体の場合は置換可能なテキストか変化するテキストを示します。 斜体は記載されている通りには入力しないテキスト、 あるいは状況に応じて変化する出力テキストを表します。 例えば、
		</para>
		<blockquote>
			<para>
				ssh を使用してリモートマシンに接続するには、 シェルプロンプトで <command>ssh <replaceable>username</replaceable>@<replaceable>domain.name</replaceable></command> と入力します。 リモートマシンが <filename>example.com</filename> であり、 そのマシンで使用しているユーザー名が john なら <command>ssh john@example.com</command> と入力します。
			</para>
			<para>
				<command>mount -o remount <replaceable>file-system</replaceable></command> コマンドは指定したファイルシステムを再マウントします。 例えば、 <filename>/home</filename> ファイルシステムを再マウントするコマンドは <command>mount -o remount /home</command> になります。
			</para>
			<para>
				現在インストールされているパッケージのバージョンを表示するには、 <command>rpm -q <replaceable>package</replaceable></command> コマンドを使用します。 結果として次を返してきます、 <command><replaceable>package-version-release</replaceable></command>。
			</para>
		</blockquote>
		<para>
			上記の太字斜体の単語 &mdash; username、 domain.name、 file-system、 package、 version、 release に注目してください。 いずれもコマンドを発行するときに入力するテキスト用のプレースホルダーかシステムにより出力されるテキスト用のプレースホルダーになっています。
		</para>
		<para>
			タイトル表示のような標準的な使用の他、 斜体は新しい重要な用語が初めて出現する場合にも使用されます。 例えば、
		</para>
		<blockquote>
			<para>
				Apache HTTP サーバーが要求を受け取ると子プロセスまたは子スレッドを発送して処理します。 この子プロセスまたは子スレッドのグループは <firstterm>サーバープール</firstterm> と呼ばれます。 Apache HTTP サーバー 2.0 では、 これらのサーバープールの作成および維持の役目は <firstterm>マルチプロセッシングモジュール</firstterm> (<firstterm>MPM</firstterm>) と呼ばれるモジュールのグループに取り込まれていました。 他のモジュールとは異なり、 MPM グループからのモジュール 1 つのみが Apache HTTP サーバー によってロードされます。
			</para>
		</blockquote>
	</section>
	
	<section>
		<title>引用における表記方法</title>
		<para>
			前後の文から視覚的に強調する目的で 2 種類のデータタイプを使用しています。 一般的には複数行になります。
		</para>
		<para>
			端末に送信される出力は <computeroutput>Mono-spaced Roman</computeroutput> にセットされるので以下のように表示されます。
		</para>
		
<screen>books        Desktop   documentation  drafts  mss    photos   stuff  svn
books_tests  Desktop1  downloads      images  notes  scripts  svgs
</screen>
		<para>
			ソースコードの一覧も <computeroutput>Mono-spaced Roman</computeroutput> でセットされますが、 以下のように強調表示されます。
		</para>
		
<programlisting language="java">package org.jboss.book.jca.ex1;

import javax.naming.InitialContext;

public class ExClient
{
   public static void main(String args[]) 
       throws Exception
   {
      InitialContext iniCtx = new InitialContext();
      Object         ref    = iniCtx.lookup("EchoBean");
      EchoHome       home   = (EchoHome) ref;
      Echo           echo   = home.create();

      System.out.println("Created Echo");

      System.out.println("Echo.echo(&#39;Hello&#39;) = " + echo.echo("Hello"));
   }
   
}
</programlisting>
	</section>
	
	<section>
		<title>注記および警告</title>
		<para>
			情報が見過ごされないよう 3 種類の視覚的なスタイルを使用して注意を引いています。
		</para>
		<note>
			<title>注記</title>
			<para>
				注記は説明している部分に対するヒントや近道あるいは代替となる手段などになります。 注記を無視しても影響はありませんが知っておくと便利なコツを見逃すことになるかもしれません。
			</para>
		</note>
		<important>
			<title>重要</title>
			<para>
				重要ボックスは見逃しやすい事項を詳細に説明しています。 現在のセッションにのみ適用される設定上の変更点、 更新を適用する前に再起動が必要なサービスなどがあります。 重要ボックスを無視してもデータを喪失するような結果にはなりませんがイライラ感やフラストレーションが生じる可能性があります。
			</para>
		</important>
		<warning>
			<title>警告</title>
			<para>
				警告は無視しないでください。 警告を無視するとデータを喪失する可能性が非常に高くなります。
			</para>
		</warning>
	</section>

</section>


